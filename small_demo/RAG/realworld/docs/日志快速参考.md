# 日志系统快速参考

## 🎛️ 命令行开关

```bash
# 默认（INFO 级别，输出到控制台+文件）
python cli.py query "问题"

# 详细模式（DEBUG 级别，看所有细节）
python cli.py query "问题" -v     # 简写
python cli.py query "问题" --verbose

# 安静模式（禁用所有日志）
python cli.py query "问题" -q     # 简写
python cli.py query "问题" --quiet

# 仅控制台（不保存日志文件）
python cli.py query "问题" --no-file

# 组合使用
python cli.py query "问题" -v --no-file    # 详细 + 不保存文件
```

---

## 🛠️ 代码中使用

```python
# 在任何模块中，都可以这样获取日志器
from realworld.logger import get_logger

logger = get_logger(__name__)
logger.info("信息消息")
logger.debug("调试消息")
logger.warning("警告消息")
logger.error("错误消息")
```

---

## ⚙️ 配置文件设置 (config.json)

```json
{
  "logging": {
    "enabled": true, // 全局开关
    "level": "INFO", // 日志级别
    "format": "%(asctime)s - %(name)s - %(levelname)s - %(message)s",
    "file_path": "logs/rag_app.log",
    "max_file_size": 10485760, // 10MB
    "backup_count": 5 // 保留5个备份
  }
}
```

---

## 🌍 环境变量

```bash
export LOG_LEVEL=DEBUG                # 日志级别
export LOGGING_ENABLED=false          # 禁用日志
export RAG_LOG_FILE=my_logs.log       # 日志文件路径
```

---

## 📊 优先级顺序

1. **命令行参数** (`-v`, `-q`, `--no-file`)
2. **环境变量** (`LOG_LEVEL` 等)
3. **配置文件** (config.json)
4. **默认值** (INFO 级别，保存到 logs/)

---

## 💾 日志文件位置

- **默认位置**: `src/realworld/logs/rag_app.log`
- **可配置**: 修改 config.json 中的 `file_path`
- **自动轮转**: 超过 10MB 自动轮转，保留 5 个备份

---

## 🎨 日志级别说明

| 级别     | 何时使用       | 示例                        |
| -------- | -------------- | --------------------------- |
| DEBUG    | 开发/调试      | `-v` flag，详细函数调用信息 |
| INFO     | 默认，一般信息 | "已添加 100 个文档"         |
| WARNING  | 潜在问题       | "缓存读取失败，使用新生成"  |
| ERROR    | 严重错误       | "Ollama 服务连接失败"       |
| CRITICAL | 应用无法继续   | 内存不足、磁盘满等          |

---

## 🚀 快速调试技巧

```bash
# 技巧 1: 查看最后 20 行日志
tail -20 logs/rag_app.log

# 技巧 2: 实时监看日志
tail -f logs/rag_app.log

# 技巧 3: 搜索特定错误
grep ERROR logs/rag_app.log

# 技巧 4: 清理旧日志
rm logs/rag_app.log*

# 技巧 5: 调试模式 + 实时日志
python cli.py query "..." -v 2>&1 | tee debug.log
```
